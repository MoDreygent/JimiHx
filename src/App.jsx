import React from 'react';

// Cleared to zero — awaiting the new specification.
// Nothing from the previous build survives here on purpose.

export default function App() {
  return <div className="shell" />;
}
